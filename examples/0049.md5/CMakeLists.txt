set(current_dir 0049.md5)

set(CMAKE_FOLDER "${CMAKE_FOLDER}/${current_dir}")

example_target(${current_dir} hello_hmac_md5)
example_target(${current_dir} hmac_md5)
example_target(${current_dir} md5)
