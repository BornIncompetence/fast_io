set(current_dir 0016.process)

set(CMAKE_FOLDER "${CMAKE_FOLDER}/${current_dir}")

example_target(${current_dir} posix_process)
example_target(${current_dir} process)
example_target(${current_dir} transmit)
