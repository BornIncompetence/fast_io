set(current_dir 0036.int128)

set(CMAKE_FOLDER "${CMAKE_FOLDER}/${current_dir}")

example_target(${current_dir} int128_t)
example_target(${current_dir} int128_t_base36)
example_target(${current_dir} int128_t_base36_lcv)
example_target(${current_dir} int128_t_hex)
example_target(${current_dir} uint128_t)
example_target(${current_dir} uint128_t_base36)
example_target(${current_dir} uint128_t_hex)
