set(current_dir 0024.locale)

set(CMAKE_FOLDER "${CMAKE_FOLDER}/${current_dir}")

example_target(${current_dir} isspace)
example_target(${current_dir} lconv_storage)
example_target(${current_dir} lcv)
example_target(${current_dir} lcv_base16)
example_target(${current_dir} lcv_double)
example_target(${current_dir} lcv_stream)
example_target(${current_dir} scan_int)
example_target(${current_dir} scan_string)
example_target(${current_dir} std_locale_bloat)
example_target(${current_dir} widen)
